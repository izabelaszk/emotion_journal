[pytest]
markers =
    integration: tests that download models, need a GPU, or hit the network

# Run the fast offline suite with:   pytest -m "not integration"
# Run everything with:               pytest
